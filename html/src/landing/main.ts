// Landing page is plain HTML/CSS. This module exists so Vite picks the page
// up as a module entry — and so a future agent has a hook for any landing-
// page interactivity (search, filtering) without retrofitting tooling.

export {};
